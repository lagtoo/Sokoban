#include <stdio.h>
void getUsername()
{
    FILE *fp;
    char inputUsername[10];
    char check[10];
    int i=0;
    printf("input name : ");
    scanf("%s",&inputUsername);



    if ((fp = fopen("username.txt","a+"))== NULL)
    {
        //fileCannotOpen();
        getUsername();
    }
    else
    {

        while(fscanf(fp,"%s",check)!=EOF)
        {
            if (inputUsername == check)//check if username exists
            {
                i=1; //if it exists i=1
                break; //should break the while loop
            }
            else //if it doesnt write to file
            {
                 fprintf(fp,"%s\n",inputUsername);
                 break;

            }
        }


    }
    fclose(fp);
    printf("Hello %s", inputUsername);
}

int main()
{
    getUsername();

    return 0;
}


//soohun 

  FILE* fp;
  char inputUsername[10]; 
  char check[10];
  int i=0;
  printf("input name : ");
  fgets(inputUsername,10,stdin); //fgets(variableName,size,standardinput)
  printf("Hello %s\n", inputUsername);
  
  if ((fp = fopen("username.txt","a+"))== NULL)
  {
    printf("FffF");
  }
  else  
  { 
     while(feof(fp)==0) //while not at end of file
     {
        if (inputUsername == check)//check if username exists
        {
          printf("Exists");
        }
        else //if it doesnt write to file
        {
          fprintf(fp,"%s\n",inputUsername);
        }
      }
  }
